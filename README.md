# BloggingApplicationUmar

## webapp front end

- Navigate to the webapp folder, in terminal type `cd webapp`
- Run the following command `npm start`

## webapi server side

- Navigate to the webapi folder, in terminal type `cd webapi`
- Run the following command `npm start`